# Bu bir ŞABLON dosyasıdır (Git'e gider, içinde gerçek sır YOKTUR).
# Kullanım: bu dosyayı ".env" adıyla kopyala ve değerleri kendine göre doldur.
#   cp .env.example .env
# Gerçek ".env" dosyası .gitignore ile Git dışında tutulur.

# --- PostgreSQL ---
POSTGRES_DB=commerce
POSTGRES_USER=commerce
POSTGRES_PASSWORD=degistir_beni
